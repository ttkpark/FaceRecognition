name: face_attendance_client
description: Flutter 클라이언트 - 얼굴 인식 출석 (서버 API 연동)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  web_socket_channel: ^3.0.2
  camera: ^0.11.0+2
  path_provider: ^2.1.4
  shared_preferences: ^2.3.3
  image_picker: ^1.0.7
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
